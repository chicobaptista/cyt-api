/*
 * import { CThemeRepository } from '@theme/repositories/theme.repository'
 * import { CreateTheme } from './create-theme'
 */

/*
 * const themeRepo = new CThemeRepository()
 * function makeCreateTheme(): CreateTheme {
 *     return new CreateTheme(themeRepo)
 * }
 */

// export default Object.freeze({ makeCreateTheme })
